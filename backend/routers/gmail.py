from fastapi import APIRouter

router = APIRouter()

# TODO (Semana 3): Gmail API
# GET  /gmail/correos          → list unclassified supplier emails
# POST /gmail/enviar           → send email as Paulet (never auto-sends)
# Gmail is read-only for classification — never modifies or moves messages
